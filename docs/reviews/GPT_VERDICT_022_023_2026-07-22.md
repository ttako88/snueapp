# GPT 판정 — PACKET 022/023 화폐분리·SR차감 (2026-07-22)

MESSAGE_ID = P-20260722-PACKET_022_023_CURRENCY_SR_REVIEW_01
- **FINAL_DECISION_022 = CONDITIONAL_PASS**
- **FINAL_DECISION_023_DB_LAYER = CONDITIONAL_PASS**
- **FINAL_DECISION_023_USER_REACHABLE = BLOCKER**
- N2_EXECUTION_AUTHORIZED = **YES_AFTER_MUST_FOR_DB_AND_FLAG_OFF_DEPLOY**
- USER_REACHABLE_ACTIVATION_AUTHORIZED = **NO**
- REQUIRED_ORDER = 022 → VERIFY → 023 → VERIFY
- ADDITIONAL_PRE_REVIEW_REQUIRED = **NO_FOR_DB** / YES_FOR_ACTIVATION_FIX

결론: 022·023의 **DB 기반 + flag-OFF 라우트 배포까지 진행 가능.** 단 flag 활성화는 차단.

## AUTHORIZED
- DB 적용 전 MUST 반영 → 022 적용·검증 → 023 적용·검증 → aiCreditCharge **서버측 OFF** 유지한 라우트 배포 → 합성계정으로 차감·환불·경합 검증 → 문제 시 라우트 우선 롤백 후 023→022 순서 rollback.

## FORBIDDEN
- **aiCreditCharge 활성화** (flag ON)
- paid SR 판매·충전·양도
- marketplace·판매자 정산·에스크로 구현
- 클라이언트가 제공한 member ID 를 신뢰한 차감
- catch 기반 즉시 환불만으로 crash-safe 라고 보고

## 질문별 판단
1. **018 예약 ↔ 023 차감**: 두 회계 분리 맞다. 권장순서: 입력·권한·idempotency 검증 → 018 예산예약 → 회원 SR 차감 → AI 생성 → 018 실제비용 정산 → 결과 commit → 성공확정. SR 부족하면 018 예약 해제. AI 호출됐으나 결과 미제공이면 회원 SR 환불하되 공급자 비용은 018에서 실발생분 정산.
2. **members FOR UPDATE**: 조건부 충분. **동일 회원 잔액을 감소시키는 모든 경로가 먼저 private.members 행을 잠근 뒤 currency 잔액 계산**해야 한다(하나라도 잠금 없이 음수 ledger 삽입하면 023끼리 경합만 막고 전체 잔액경합 못 막음). → **unlock_subject(011) 등 다른 SR 차감 경로도 members 잠그는지 확인 필요.** 환불(양수 추가)은 직렬화 필수 아니나 원본 reversal 중복방지 별도 필요(023은 idem_key로 함).
3. **환불 경로**: 빈 결과 환불 맞다. 단 3경로 열거로 불충분 — 아래도 동일 계약에 포함:
   파싱·스키마 검증 실패 / moderation·refusal 로 본문 없음 / 결과 저장·최종 commit 실패 /
   외부 호출 timeout / 서버 프로세스 종료 / 응답 전송 상태 불확정.
   **환불 기준 = 예외 이름이 아니라 `DELIVERABLE_COMMITTED=false`.** 클라 연결 종료만으로
   이미 저장된 결과를 환불해서도 안 된다.
4. **법적판단(정정)**: "사업자등록 없으면 유료크레딧·에스크로 다 불가"는 **과도**. 유료 온라인
   서비스 실제 판매 전엔 사업자등록·통신판매 의무·세무·환불·약관·결제사업자 조건 별도 검토.
   **그러나 내부 원장에 paid currency 미리 분리하는 것 자체는 판매행위 아님(022 안전).**
   SNUE 직접제공·양도불가 크레딧이면 "발행자 직영점 상거래 선수금"에 가까울 가능성(추론, 확정
   법률판단 아님). 에스크로도 거래액·결제방식에 따라 다름.

## ACTIVATION BLOCKER (flag ON 전제) — 미래 작업
현재 설계 구멍: SR 차감 성공 → 프로세스 종료·배포·강제 timeout → catch·환불 RPC 미실행 →
**결과 없이 SR만 영구 차감.** 따라서 활성화 전:
- **durable operation 상태기계** 필요: 최소 CHARGED / GENERATING / DELIVERABLE_COMMITTED /
  REFUNDED. 원본 차감 entry + 018 reservation + request id 결합.
- **stale reconciliation**: 일정 시간 미종결 건을 재조회·환불하는 작업.
- flag 활성화(+AI 키)는 이게 된 뒤. AI 키 자체도 held(소유자).

## 처리
- DB(022→023)는 적용 가능(flag OFF). ADDITIONAL_PRE_REVIEW=NO_FOR_DB → MUST 반영 후 재검수
  없이 적용. **명시 DB MUST 재확인**(members-lock 경로, reason/sign CHECK 무해) 후 022→검증→023→검증.
- **flag(aiCreditCharge)는 OFF 유지.** 활성화는 durable 상태기계 + AI 키 뒤(별도, 소유자).
- 라우트 refund-완전성(DELIVERABLE_COMMITTED 기준)은 활성화 전 구현 — 미래 작업.
- 아침 보고: E는 "DB 적용됨, 개인별 SR 차감은 flag OFF라 아직 미작동. 켜려면 durable 상태기계
  구현 + AI 키 필요. GPT가 timeout 시 SR 영구차감 위험으로 활성화 차단."
