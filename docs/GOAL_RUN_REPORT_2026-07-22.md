# goal 실행 결과 보고 — 2026-07-22

> 사장님이 A~G 승인 + goal 모드로 지시한 것의 실행 결과입니다. "잘했네" 또는
> "이건 롤백" 만 하시면 됩니다. 세부·롤백수단은 각 항목에 붙였습니다.
> 밤작업 자체는 `MORNING_REPORT_2026-07-23.md`, 진행 원본은 `COLLAB_STATE.md`.

## 한 줄
**승인하신 A~E를 GPT 검수 게이트를 통과시켜 전부 운영 DB에 반영했고, 인증 보안수정(A)은
배포까지 마쳤습니다.** 검수가 적용 *전에* 결함을 여러 개 잡아 프로덕션 도달을 막았습니다.
모든 신기능 스위치(flag)는 안전상 아직 꺼둔 상태입니다.

## 운영에 반영된 것 (전부 되돌릴 수 있음)

| 항목 | 무엇을 했나 | 상태 |
|---|---|---|
| **A 020** 인증 새치기 방지 | 인증서류 확정 단계의 동시성 보안구멍을 token-fence로 닫음. **적용+배포**(9fa001d→e6923dc), smoke 통과 | 살아있음 |
| **B 016** 강의평가 통계 | 3명짜리 통계가 10명짜리로 보이던 프라이버시 결함 수정. 화면 스위치는 OFF | DB만 |
| **C 019** 실습학교 배정 | 학기별 배정 저장 DB. 소수학교 인원은 익명화(3명 미만 "<3"). 스위치 OFF | DB만 |
| **D 021** 고아 파일 탐지 | 주인 없는 인증서류 파일을 "찾기만" 하는 읽기전용 도구(삭제 없음) | DB만 |
| **E 022·023** 포인트 장부·지도안 요금 | SR 장부를 무료/유료로 분리(지금 해야 소급 정확), 지도안 SR 차감 기능. 스위치 OFF | DB만 |

운영 함수 118→134개(+16), 데이터 한 건도 안 바뀜, 권한 경계 정상.

## GPT 검수가 적용 전에 잡아준 것 (안 잡았으면 운영에 갔을 결함)
- **020**: BLOCKER 3번 — ① TTL 선점이 지연된 작업자를 못 막음 ② 경로결합·상태전이가 원자적이지 않음 ③ 레거시 우회 RPC. 세 번 고쳐 token-fenced 원자적 구조로.
- **016**: 응답 0인 항목 키 누락, 저분모 노출, 종단 차분공격.
- **021**: 정본 경로·token 누출 위험 → 일치여부만 반환하도록.
- **022**: 트리거 함수가 실수로 전체공개 실행권한을 가짐(사후검증이 잡음) → 회수.
- **019**: 소수학교 정확 인원 노출, "인증"으로 오인 표시.

## ⏸️ 스위치를 아직 안 켠 이유 (전부 GPT가 정당한 이유로 활성화 차단)
- **강의평가(courseReview)**: 실시간 통계라 값이 바뀌는 걸로 새 응답을 역추론할 수 있음(차분공격). 켜기 전 "스냅샷 공개"로 바꿔야 함.
- **지도안 SR 차감(aiCreditCharge)**: 지금 켜면 AI 생성 중 서버가 죽으면 SR이 환불 없이 영구 차감될 수 있음. 켜기 전 "durable 상태기계" 필요. + **AI 자체가 아직 비활성**(Gemini 키 미등록 — 사장님만 등록 가능).
- **실습학교(practicumPlacement)**: 배정 저장 화면은 켤 수 있으나, 학교별 게시판 글쓰기는 "원자적 게시 게이트 + 게시판층 제한(잠금 후 24h 냉각 등)" 후에. 스위치가 게시판 진입도 여는지 확인 필요해 일단 OFF.

→ 이건 "안 됨"이 아니라 "한 단계 더 있어야 켜도 안전"이라는 뜻입니다. 각 선행작업은 별도 진행.

## ⏸️ 사장님 한마디면 되는 것 (held)
1. **F/015 로그인 개방** — 부재중 인증경계 변경이라 안 건드렸습니다. 계실 때 "지금 열자" 하시면 10초.
2. **AI 활성화** — Gemini 키 등록은 사장님만 가능(TTY). 켜기 전 위 durable 상태기계.
3. **프롬프트 v3 A/B**(유료 실측) — 별도 승인.

## 실증 못 한 것 (정직하게)
- **020 동시성 라이브 재현**: 코드는 GPT 3회 검수 통과 + 배포 + smoke PASS. 다만 실제 동시 재현은 운영에 합성 인증신청/계정(보호데이터, 야간금지 N3)을 만들어야 해서 **이연**했습니다. dev나 사장님 입회로 재현 권합니다.
- **016 통계 출력 실측(n=0,9,10)**: 로직은 검수 통과. 합성 리뷰가 필요해 이연(강의평가 OFF).

## 롤백 방법 (원하시면)
- 전체 배포 롤백: origin/main 을 `9fa001d`로 되돌리면 A 배포 전으로. (단 020 DB는 남으므로, 되돌리려면 finalize 라우트만 문제 — 그 경우 알려주시면 정확히 처리)
- 개별 마이그레이션: 전부 추가형이라 되돌림 가능. 데이터 지우는 되돌림은 안 함(N3).

## 다음
- 로컬 GPT가 단원구성.csv 성취기준코드를 채우면 반영·검증(`npm run lesson:check`) — 데이터 도착 대기.
- 각 flag 활성화 선행작업(snapshot 공개 / durable 상태기계 / atomic 게시 게이트)은 별도 착수.
