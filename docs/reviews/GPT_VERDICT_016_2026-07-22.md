# GPT 판정 — PACKET 016 통계 항목별 분모 (2026-07-22)

MESSAGE_ID = P-20260722-PACKET_016_STATS_DENOMINATOR_REVIEW_01
**FINAL_DECISION = CONDITIONAL_PASS / N2_EXECUTION_AUTHORIZED = YES_AFTER_MUST
/ USER_REACHABLE_ACTIVATION_AUTHORIZED = NO**

항목별 실제 응답자 수 분모 방향은 맞다. 다만 현재 SQL이 "모자란 항목도 키 유지"
계약을 어긴다.

## 질문별 판단
1. `n_item>=10`: 적절, 유지. (임계 높여도 시간차 공격 해결 안 됨)
2. 정확한 n_item 공개: 분포 공개 항목은 합계로 계산되므로 OK. 그러나 `n_item<10`
   숨김 항목은 정확 수 대신 `n=null, n_band="below_10"`.
3. hidden_reason: `too_few_answers`/`sparse_cell` 구분은 불필요 →
   전부 `insufficient_data` 하나로 통합.
4. 응답구조 변경: 억지 호환 말고 `schema_version=2` 명시. 활성화 전 화면 v2 맞춤 + 계약테스트.

## MUST — 적용 전 (SQL 수정)
1. **고정 4항목 목록을 먼저 만들고 per_item을 LEFT JOIN → 응답자 0 항목도 반드시 반환.**
   (현재 per_item은 cells 존재 항목만 생성 → 0응답 항목은 키 자체가 사라짐)
2. 숨김 항목의 정확한 n_item 비노출.
3. 숨김 사유 `insufficient_data` 하나로 통합.
4. 응답에 `schema_version: 2`.
5. `disclosure='full'`인데 일부 항목 숨겨질 수 있으니 `partial`/`item_level`처럼 오해 없는 값으로.
6. 현재 모든 코드·RPC 소비자 검색해 구형 응답구조 의존 확인.
7. 적용 후 네 항목 각각 n=0,9,10·최소셀 2/3·일부 답변 누락을 기계적 검증.

## ACTIVATION_BLOCKER (이번 DB 적용과 분리 — 미래 게이트)
실시간 재계산이라 **종단 차분 공격** 가능: 공개 결과 {A:5,B:5}→{A:6,B:5}면 새
응답이 A였음을 시간차로 역추론. `n_item>=10`·`min_cell>=3`은 이걸 못 막음.
→ **교체 SQL은 적용해도 되나(N2), courseReview flag ON(활성화) 전에 공개 통계를
실시간 계산이 아닌 snapshot 방식으로 바꿔야 한다.** flag는 A~G에서 계속 OFF라
지금 무관. courseReview 켜기 전 선행조건으로 기록.

## 처리
- stats 함수에 MUST 1~5 반영 → dry-run → GPT 재검수(rev2 필요할 수도) → 적용(flag OFF).
- MUST 6(소비자 검색)·7(검증)은 적용 전후 수행.
- ROLLBACK = create or replace 로 011 원본 재실행.
